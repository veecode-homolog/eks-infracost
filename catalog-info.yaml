apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: eks-infracost-location
  description: Location to import all eks-infracost catalog components
spec:
  targets:
    - ./.platform/*.yaml